# Example configuration

`ARModelGeneration/appsettings.json` contains an invalid example Meshy token. Supply your own token locally (for example through the MeshyToken environment setting) and configure the Meshy endpoint before running requests.

Values containing `EXAMPLE`, zero-only identifiers and `example.com` domains are placeholders, not real credentials. Never use these example credentials in production.
